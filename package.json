{
  "name": "midt",
  "version": "3.0.0",
  "description": "Design Tokens for MapsIndoors",
  "author": "MapsPeople A/S",
  "license": "CC-BY-4.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/mapsindoors/midt.git"
  },
  "keywords": [
    "mapsindoors",
    "design",
    "tokens",
    "design",
    "system"
  ],
  "sass": "mixins/_index.scss",
  "scripts": {
    "sass": "sass style.scss:style.css --watch --no-source-map",
    "build": "npm run clean && node_modules/.bin/style-dictionary build",
    "clean": "rm -rf build",
    "watch": "npm run build && chokidar \"properties/**/*.json\" -c \"npm run build\"",
    "postversion": "git push -u origin && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "sass": "^1.23.7",
    "style-dictionary": "^2.8.3"
  }
}
